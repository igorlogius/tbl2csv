{
    "name": "tbl2csv",
    "description": "Save tables or lists as a comma seperated values (csv) file which can be opened with any spreadsheet processor (e.g. excel, libreoffice-calc) to do further processing and conversion (e.g. to xls or ods)",
    "version": "1.2.8",
    "manifest_version": 2,
    "icons": {
	"128": "icon.png"
    },
    "background": {
        "scripts": ["background.js"]
    },
    "permissions": [
        "menus",
	"activeTab"
    ],
    "browser_action": {
        "default_icon": "icon.png",
        "default_title": "highlight exports"
    }
}
